# LOCA Agent Notes

- This is a greenfield project named `loca-social-mvp`.
- Use `npm` in this workspace because `pnpm` is not installed locally.
- Do not copy code or data from other LOCA repositories or worktrees.
- Demo mode is the default for local development and uses Korean fixtures with localStorage.
- Supabase mode must not display fake success when required credentials are missing.
- Keep UI Korean-first, mobile-first, dark-first, and place-first.
